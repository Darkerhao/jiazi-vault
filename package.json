{
  "name": "jiazi-vault",
  "private": true,
  "version": "0.1.0",
  "description": "Local encrypted credential vault for developers",
  "type": "module",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "vite",
    "electron:compile": "tsc -p tsconfig.electron.json",
    "electron:dev": "pnpm electron:compile && concurrently --kill-others-on-fail \"vite\" \"wait-on http://127.0.0.1:1420 && electron .\"",
    "electron:build": "pnpm build && electron-builder",
    "electron:build:win": "pnpm build && electron-builder --win --x64 --config.electronDist=node_modules/electron/dist",
    "build": "pnpm typecheck && vite build && pnpm electron:compile",
    "typecheck": "vue-tsc --noEmit",
    "test": "pnpm electron:compile && node --test tests/*.test.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@node-rs/argon2": "2.2.0",
    "@vicons/ionicons5": "^0.13.0",
    "naive-ui": "^2.41.0",
    "pinia": "^3.0.3",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "concurrently": "^10.0.5",
    "electron": "^44.2.0",
    "electron-builder": "^26.15.3",
    "typescript": "^5.8.3",
    "vite": "^7.1.3",
    "vue-tsc": "^3.0.6",
    "wait-on": "^9.1.0"
  },
  "build": {
    "appId": "com.jiazi.vault",
    "productName": "Jiazi Vault",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**",
      "dist-electron/**",
      "package.json"
    ],
    "win": {
      "target": [{ "target": "nsis", "arch": ["x64"] }]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "runAfterFinish": false
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
